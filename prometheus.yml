global:
  scrape_interval: 5s # Scrape metrics every 5 seconds for responsive charts

scrape_configs:
  - job_name: 'research-bot'
    static_configs:
      - targets: ['host.docker.internal:8000'] # Points to the python app's prometheus exporter
